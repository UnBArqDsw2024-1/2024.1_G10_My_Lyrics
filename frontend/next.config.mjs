/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ytimg.com",
      "my-lyrics.learnwithpadas.com",
      "akamai.sscdn.co",
      "localhost",
    ],
  },
};

export default nextConfig;
